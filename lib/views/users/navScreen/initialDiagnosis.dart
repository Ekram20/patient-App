import 'package:flutter/material.dart';

class InitialDiagnosis extends StatelessWidget {
  const InitialDiagnosis({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Initial Diagnosis Screen'),
    );
  }
}
